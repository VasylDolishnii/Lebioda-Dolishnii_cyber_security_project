# Phase 3 – Local Agent Setup and Configuration

## 1. Local Operating System & Hostname Preparation

Verify the pre-installed Ubuntu environment on the host laptop (HP ProBook 640 G1).
Optimize the terminal prompt and local hostname for improved CLI readability.
Ensure local network host resolution functions properly without throwing errors.
Prepare the operating system for agent package installation and background services.

### Terminal Hostname Optimization
```bash
sudo hostnamectl set-hostname hp
